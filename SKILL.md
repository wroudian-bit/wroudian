---
name: studio-namma-style
description: 用 Studio Namma 风格创建高端创意工作室、品牌设计、Webflow、作品集或数字体验网站。适用于需要暗色沉浸感、大字号排版、项目视频、服务矩阵、强 CTA 和精致滚动动效的页面设计与前端实现。
---

# Studio Namma 风格网站技能

## 何时使用

当用户提出以下需求时使用本技能：

- 喜欢 `studionamma.com`，希望做相似风格的网站
- 创建创意工作室、设计公司、品牌代理、Webflow 开发团队官网
- 制作高级作品集或案例展示页
- 需要暗色、高级、大标题、视频感、强动效的网站
- 需要把品牌、设计、开发、内容服务包装成一个有记忆点的体验

不要用于：

- 后台管理系统
- 电商商品列表
- 新闻门户
- 需要大量表格和数据录入的应用
- 严肃政府、医疗、金融合规页面

## 设计目标

产出应具有以下感受：

- 像高端创意工作室
- 首屏像海报或 showreel 开场
- 文字有节奏，短而锋利
- 项目作品比自我介绍更重要
- 动效精确、克制、有完成度
- 移动端仍然保留强烈视觉个性

## 默认页面结构

如果用户没有指定结构，使用以下首页结构：

1. `Header`：品牌名、导航、地点、联系入口
2. `Hero`：超大标题、辅助标语、滚动提示
3. `Showreel`：视频或大幅项目视觉
4. `Manifesto`：短句品牌宣言
5. `SelectedProjects`：精选项目列表与预览
6. `Services`：服务词组矩阵
7. `ContactCTA`：强 CTA 与联系表单
8. `Footer`：导航、社交链接、邮箱、地区

## 视觉配方

### 色彩

使用深色基底和高对比文字：

```css
:root {
  --bg: #050505;
  --text: #f4f1e8;
  --muted: #9b9b9b;
  --line: rgba(255, 255, 255, 0.14);
  --accent: #ffffff;
}
```

少量强调色可以来自项目图片或视频，不要额外制造复杂配色。

### 字体

优先选择几何感无衬线字体：

- 英文：Inter Tight、Archivo、Neue Haas Grotesk、Suisse Intl、PP Neue Montreal
- 中文：思源黑体、HarmonyOS Sans、阿里巴巴普惠体、苹方

### 排版

Hero 标题建议：

```css
.hero-title {
  font-size: clamp(64px, 11vw, 180px);
  line-height: 0.9;
  letter-spacing: 0;
  max-width: 12ch;
}
```

正文建议：

```css
.body-copy {
  font-size: clamp(16px, 1.3vw, 22px);
  line-height: 1.35;
}
```

规则：

- 大标题要敢大
- 行高要紧
- 字距不要为负
- 说明文字要短
- 模块间距要大

## 文案模板

### Hero 中文

```text
为野心品牌
打造高级
数字体验
```

辅助语：

```text
We think craft and design
```

### 宣言

```text
这从来不只是一个网站。
每个细节都算数。
我们打造能被记住的数字体验。
你的设计，是我们的执念。
你的品牌，是我们的游乐场。
```

### CTA

```text
一起做点不普通的东西
如果你不想要平均水平，我们应该聊聊。
把想法丢过来，我们把它做出来。
```

### 服务

```text
艺术指导
品牌叙事
品牌识别
Webflow 开发
UI/UX 设计
Figma 原型
GSAP 动效
页面转场
创意广告
SEO 与内容
```

## 组件清单

实现时优先拆成以下组件：

- `SiteHeader`
- `HeroStatement`
- `ShowreelBlock`
- `ManifestoText`
- `SelectedProjects`
- `ProjectPreview`
- `ServiceMatrix`
- `ContactCTA`
- `ContactForm`
- `SiteFooter`

## 交互模式

推荐实现：

- 首屏标题逐行 reveal
- 页面进入时轻微淡入
- 滚动到模块时文字上移出现
- 项目 hover 时显示视频或图片预览
- 服务标签 hover 反色
- CTA hover 有明确反馈

如果使用 GSAP：

- 用 `ScrollTrigger` 控制滚动进入
- 用 timeline 管理首屏入场
- 移动端减少动画距离和数量
- 支持 `prefers-reduced-motion`

## 响应式策略

### 桌面端

- Hero 占据首屏主要面积
- 作品视频或预览大面积展示
- 服务矩阵可 3 到 4 列
- 导航完整显示

### 移动端

- Hero 保持大，但控制断行
- 导航折叠
- 视频和图片全宽
- 项目列表纵向排列
- 服务矩阵 1 到 2 列
- 表单字段单列

移动端不得出现文字溢出、按钮挤压、视频遮挡标题等问题。

## 实现步骤

1. 读取现有项目结构和技术栈。
2. 找到主页面入口和全局样式文件。
3. 建立深色视觉变量和基础排版规则。
4. 搭建首页模块：Hero、Showreel、Manifesto、Projects、Services、CTA、Footer。
5. 加入真实媒体资源；如果没有资源，使用高质量占位图并保留视频接口。
6. 加入动效，先保证布局稳定，再增强滚动体验。
7. 检查桌面端和移动端。
8. 运行构建或本地预览。

## 验收清单

完成前逐项检查：

- 首屏是否一眼能看出品牌调性
- 大标题是否在各尺寸下稳定
- 是否有真实作品或媒体视觉
- 服务区是否清楚但不冗长
- CTA 是否明确
- 导航和页脚是否完整
- 动效是否克制
- 移动端没有文字重叠或横向滚动
- 页面不依赖装饰渐变撑场面

## 常见错误

避免以下做法：

- 做成普通 SaaS 落地页
- 用大量圆角卡片堆模块
- 用渐变背景代替真实视觉
- 服务介绍写成长篇段落
- 作品区过小
- 首屏标题不够大胆
- 动效太多但没有内容支撑
- 移动端直接缩小桌面布局

## 最小可行输出

如果时间有限，至少完成：

- 一个强 Hero
- 一个视频或大图 Showreel
- 一组宣言短句
- 一个精选项目列表
- 一个服务矩阵
- 一个强 CTA

这六部分足以形成 Studio Namma 风格的基本骨架。
